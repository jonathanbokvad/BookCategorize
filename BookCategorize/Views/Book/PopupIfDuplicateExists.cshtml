@if (true)
    {
        <script type="text/javascript">  
            window.onload = function () {
                location.href = '@Url.Action("Index", "Book")';
                alert("This book is already saved in my books!");
            };
        </script>
    }
